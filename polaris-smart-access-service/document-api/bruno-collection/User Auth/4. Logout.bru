meta {
  name: 4. Logout
  type: http
  seq: 6
}

post {
  url: {{dev}}/auth/logout
  body: json
  auth: inherit
}

headers {
  X-Session-Token: {{session_token}}
  user-username: {{username}}
  Content-Type: application/json
}

body:json {
  {}
}

settings {
  encodeUrl: true
}
